/**
 * Practicing with pointers
 * 
 */


#include<stdio.h> // printf and scanf



/*practice...
int main()
{
    int a;
    int *b;
    // This small snippet of code is how to store an integer value
    // using a pointer and printing the value stored with the pointer.
    
    // set a to a value. the lasted value of a is what will be stored into b.
    a = 67;
    a = 58;
    // storing the address of a into our b pointer
    b = &a;
    // printing the value that is store at the pointer b which is whatever is saved
    // into the varibale a.
    printf("%d\n", *b);

    // update a to see the print statement change.
    a = 40;
    printf("%d\n", *b);

}
*/

